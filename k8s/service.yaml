apiVersion: v1
kind: Service
metadata:
  name: code-healer-service
  namespace: code-healer
  labels:
    app: code-healer
spec:
  type: NodePort
  selector:
    app: code-healer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
