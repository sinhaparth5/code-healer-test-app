apiVersion: v1
kind: Service
metadata:
  name: {{ include "code-healer.fullname" . }}
  labels:
    {{- include "code-healer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "code-healer.selectorLabels" . | nindent 4 }}
